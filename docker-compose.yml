version: '3.8'

services:
  stats-db:
    image: postgres:14-alpine
    ports:
      - "6541:5432"
    environment:
      - POSTGRES_DB=explore-with-me-stats
      - POSTGRES_USER=iamroot
      - POSTGRES_PASSWORD=iamroot

  ewm-db:
    image: postgres:14-alpine
    ports:
      - "6542:5432"
    environment:
      - POSTGRES_DB=explore-with-me
      - POSTGRES_USER=iamroot
      - POSTGRES_PASSWORD=iamroot

  ewm-service:
    build: ./main-server
    ports:
      - "8080:8080"
    depends_on:
      - ewm-db
      - stats-server
    environment:
      - SERVER_PORT=8080
      - STATS-SERVER_URL=http://stats-server:9090
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-main:5432/explore-with-me
      - POSTGRES_USER=iamroot
      - POSTGRES_PASSWORD=iamroot

  stats-server:
    build: ./stats-server
    ports:
      - "9090:9090"
    depends_on:
      - stats-db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-stats:5432/explore-with-me-stats
      - POSTGRES_USER=iamroot
      - POSTGRES_PASSWORD=iamroot
      - SERVER-PORT=9090